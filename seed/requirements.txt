# curl_cffi impersonates a real browser's TLS fingerprint. Yahoo's
# query1 API blocks python-requests on sight but serves real-browser
# traffic. We hit Yahoo's chart endpoint directly (same endpoint the
# yfinance library uses) instead of going through yfinance, because
# yfinance 0.2.x has a Session-object bug with newer curl_cffi releases.
curl_cffi==0.7.4
pandas==2.2.3
psycopg[binary]==3.2.3
python-dotenv==1.0.1
tenacity==9.0.0
