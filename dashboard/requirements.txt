dash==2.18.2
dash-bootstrap-components==1.7.1
plotly==5.24.1
httpx==0.28.1
pandas==2.2.3
# Production WSGI server. Dash's built-in Flask dev server is fine for
# local iteration but warns in its own logs not to use in prod.
gunicorn==23.0.0
